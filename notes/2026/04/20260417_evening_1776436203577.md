# 🌙 Evening Dev Note

**Date:** Friday, April 17, 2026
**Time:** 02:30 PM
**Mood:** Reflection mode 🌿

---

## 🔍 End-of-Day Reflection

Wrapping up today, I reflected on optimizing a backtest pipeline, which was crucial for improving the efficiency of our trading strategy evaluation. This optimization was important because it allowed us to run multiple backtests in parallel, reducing the overall processing time and enabling us to make data-driven decisions more quickly. I realized that by leveraging multiprocessing and refining our data fetch queries, we could significantly enhance the pipeline's performance.

```python
import pandas as pd
import multiprocessing

def fetch_data(symbol):
    # fetch data from database
    return pd.read_sql_query(f"SELECT * FROM prices WHERE symbol = '{symbol}'", db_connection)

def backtest(symbol):
    data = fetch_data(symbol)
    # perform backtest calculations
    return data.apply(calculation_function)

if __name__ == '__main__':
    symbols = ['AAPL', 'GOOG', 'MSFT']
    with multiprocessing.Pool() as pool:
        results = pool.map(backtest, symbols)
    # combine and analyze results
```

By applying multiprocessing to our backtest pipeline, we reduced the execution time by over 50%, allowing us to evaluate more strategies and iterate on our approach more rapidly. The key change was shifting from sequential execution to parallel processing, which better utilized our system's resources and minimized idle time. This improvement has significantly enhanced our ability to refine and deploy new trading strategies.

Tomorrow, I plan to dive into SQL query performance tuning to further optimize our data retrieval processes.

---

*Part of my [Daily-Upload](https://github.com/gaurav712-gujjar/Daily-Upload) streak — 3 notes a day, every day.*
