# 🌤️ Afternoon Dev Note

**Date:** Saturday, April 11, 2026
**Time:** 06:31 PM
**Mood:** Build mode 🔨

---

## 🛠️ What I Built / Debugged

Been working on building a trading bot signal system, specifically designing a component that generates buy and sell signals based on a moving average crossover strategy. The goal is to create a system that can accurately identify trends and reversals in the market, allowing the bot to make informed trading decisions. I've been focusing on implementing the logic for calculating the short and long term moving averages, and using their crossovers to generate signals.

```
def calculate_moving_averages(prices, short_window, long_window):
    short_ma = prices.rolling(window=short_window).mean()
    long_ma = prices.rolling(window=long_window).mean()
    return short_ma, long_ma

def generate_signals(prices, short_window, long_window):
    short_ma, long_ma = calculate_moving_averages(prices, short_window, long_window)
    signals = pd.DataFrame(index=prices.index)
    signals['signal'] = 0
    signals.loc[(short_ma > long_ma) & (short_ma.shift(1) < long_ma.shift(1)), 'signal'] = 1
    signals.loc[(short_ma < long_ma) & (short_ma.shift(1) > long_ma.shift(1)), 'signal'] = -1
    return signals
```

Initially, I encountered issues with the signal generation logic, as it was not correctly handling the cases where the moving averages were equal, resulting in incorrect signals being generated. I fixed this by adding additional conditions to the signal generation logic to handle these edge cases. I also had to adjust the calculation of the moving averages to ensure that the windows were correctly aligned with the price data.

I still need to integrate this signal generation component with the rest of the trading bot system, which will involve connecting it to a data feed and a trading engine.

---

*Part of my [Daily-Upload](https://github.com/gaurav712-gujjar/Daily-Upload) streak — 3 notes a day, every day.*
