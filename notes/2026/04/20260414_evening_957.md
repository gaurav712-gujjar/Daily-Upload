# 🌙 Evening Dev Note

**Date:** Tuesday, April 14, 2026
**Time:** 02:30 PM
**Mood:** Reflection mode 🌿

---

## 🔍 End-of-Day Reflection

Wrapping up today, I reflected on optimizing a backtest pipeline, which was important because it directly impacts the performance and reliability of our trading strategy. I realized that a significant bottleneck was the repeated calculation of technical indicators, which could be avoided by storing and reusing previously computed values. This optimization is crucial as it enables us to test more strategies and scenarios without wasting computational resources.

```
def calculate_indicators(data):
  indicators = {}
  for column in data.columns:
    if column.startswith('indicator_'):
      indicators[column] = data[column].apply(lambda x: calculate_indicator(x))
  return indicators

# Before: calculating indicators for each row separately
# data.apply(calculate_indicator, axis=1)

# After: calculate indicators only when necessary and store results
indicator_cache = {}
def calculate_indicator(value):
  if value not in indicator_cache:
    indicator_cache[value] = calculate_indicator_impl(value)
  return indicator_cache[value]
```

By implementing a caching mechanism, I significantly reduced the number of redundant calculations, resulting in a substantial speedup of the backtest pipeline. The before and after comparison shows a notable difference in performance, with the optimized version being several times faster. This improvement is a direct result of identifying and addressing the bottleneck, and it has greatly enhanced the overall efficiency of our strategy testing process.

Tomorrow, I plan to delve into improving a data preprocessing step to further streamline our workflow.

---

*Part of my [Daily-Upload](https://github.com/gaurav712-gujjar/Daily-Upload) streak — 3 notes a day, every day.*
