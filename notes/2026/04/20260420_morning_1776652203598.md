# ☀️ Morning Dev Note

**Date:** Monday, April 20, 2026
**Time:** 02:30 AM
**Mood:** Learning mode 🧠

---

## 💡 What I Learned Today

Today I learned about using scikit-learn Pipeline with ColumnTransformer to simplify my machine learning workflows by combining multiple data transformations and a model into a single pipeline. This matters because it allows me to easily test and compare different models and transformations on my dataset, and also ensures that the same transformations are applied to both my training and testing data. By using a pipeline, I can also avoid data leakage and ensure that my model generalization is improved.

```
from sklearn.pipeline import Pipeline
from sklearn.compose import ColumnTransformer
from sklearn.preprocessing import StandardScaler, OneHotEncoder
from sklearn.ensemble import RandomForestClassifier
from sklearn.model_selection import train_test_split
import pandas as pd

# assume X is a dataframe with numeric and categorical columns
numeric_features = X.select_dtypes(include=['int64', 'float64']).columns
categorical_features = X.select_dtypes(include=['object']).columns

preprocessor = ColumnTransformer(
    transformers=[
        ('num', StandardScaler(), numeric_features),
        ('cat', OneHotEncoder(), categorical_features)
    ]
)

pipeline = Pipeline([
    ('preprocessor', preprocessor),
    ('classifier', RandomForestClassifier())
])
```

What surprised me was how easily I could switch between different models and transformations in the pipeline, and how it simplified my code by avoiding the need to manually apply transformations to my data before training a model. I also need to watch out for the order of transformations in the pipeline, as some transformations may require the output of previous ones. I found that debugging a pipeline can be more challenging than a single model, so I need to be careful when using it.

I want to explore next how to tune the hyperparameters of a pipeline using grid search or cross-validation to further improve the performance of my model.

---

*Part of my [Daily-Upload](https://github.com/gaurav712-gujjar/Daily-Upload) streak — 3 notes a day, every day.*
